1.Build
	You cannot use strong name certificate setting.
	Please remove setting from each projects.

2.NuGet batch
	Need to build "Release" ones if even you want to use "Debug" because
	Build event expects release folders.
